package DS.Codiblity;

public class FishAlive {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		 // write your code in JavaScript (Node.js 6.4.0)
		/*
		 * int[] upstream =; int downstream = 0;
		 * 
		 * for( let i = 0; i < A.length; i++ ) { let currFish = A[i]; if(B[i]
		 * === 1) { upstream.push(currFish); } else { if(upstream.length > 0) {
		 * while(upstream.length > 0 && currFish > upstream[upstream.length-1])
		 * upstream.pop(); if(upstream.length == 0) downstream++; } else
		 * downstream++; } }
		 * 
		 * return upstream.length + downstream;
		 */

	}

	
	
}
